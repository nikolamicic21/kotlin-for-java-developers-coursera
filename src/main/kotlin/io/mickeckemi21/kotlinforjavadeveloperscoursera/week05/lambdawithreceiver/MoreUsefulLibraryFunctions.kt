package io.mickeckemi21.kotlinforjavadeveloperscoursera.week05.lambdawithreceiver

private fun main() {

    // with - return result of lambda, { this }
    // run - return result of lambda, { this }
    // apply - return receiver , { this }

    // let - return result of lambda, { it }
    // also - return receiver, { it }

}