package io.mickeckemi21.kotlinforjavadeveloperscoursera.week04.oop

private fun main() {

    // public (by default), private
    // protected (only in sub-classes), internal (instead of package-private)
    // final (by default), open
    // abstract, override (mandatory)



}